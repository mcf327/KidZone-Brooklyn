<%- include('../partials/header') %>

<h2>All User-Added Locations</h2><br><br>
<div id="list-all">
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Address</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <% locations.forEach(function(location) { %>
                <tr>
                    <td><%= location.name %></td>
                    <td><%= location.address %></td>
                    <td><a href="/locations/<%= location._id %>">Details</a></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</div>
<%- include('../partials/footer') %>